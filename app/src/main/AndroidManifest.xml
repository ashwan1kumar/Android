<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.krashwani0908.firegram">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/index"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/index"
        android:supportsRtl="true"
        android:theme="@style/Theme.Firegram">
        <activity android:name=".CreateActivity"></activity>
        <activity android:name=".ProfileActivity" />3
        <activity android:name=".PostsActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>